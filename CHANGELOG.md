# 更新日志

本项目每次更新改了什么，都记在这里。版本号与页面右上角显示的 `vX.Y.Z` 一致。

---

## [0.1.4] — 2026-09-24

### 新增 · 优先级排行榜

把「钱该花在哪」从一句话变成一张可排序的榜单。

- 空间页新增入口卡「XX 的钱该花在哪」，进去是独立的排行榜页
- **三张榜分开生成**：硬装 · 隐藏 / 硬装 · 表面 / 软装
- 每条按**五个维度**打分（返工代价 / 安全影响 / 替换难度 / 使用频率 / 感知强度），
  加权算分后排成四个梯队：**S 做错要砸掉重来 · A 重点花钱 · B 够用就行 · C 可以后补**
- **四种预设权重**：均衡 / 家有老人小孩 / 预算紧 / 追求品质 —— 切换只本地重算，不发任何请求
- 结果**按空间缓存在本机**，切 tab、切预设、下次进入都不重新花钱
- 每条可展开看「为什么 / 关键数字 / 造价区间 / 做错的代价 / 怎么验」，
  并能「问设计师」（带着这条跳进对话）或「复制这一条」
- **不漏项的地基**：每个空间的「必须覆盖清单」（`RANK_COVERAGE`）原样塞进 prompt 要求逐条覆盖
- 返回的 JSON 一律先过 `sanitizeRank` 校验：分数夹到 0~10、校验造价区间与上下限、
  说「规范」却没给标准号的一律降级为「经验」、条目太少直接判无效
- 面包屑、应用栏标题、返回键、Esc 全部接入排行榜页

### 修复

- 空间页的 `class="chips row"` 写错了 —— `chips` 与 `row` 本该是同一个元素的
  两个 class，应为 `"chips.row"`

---

## [0.1.3] — 2026-09-24

### 新增 · 账号与云端历史

- **注册 / 登录**：接入 Supabase，邮箱 + 密码，走 GoTrue。登录态存在本机，
  下次打开用 `refresh_token` 免密续期并自动拉回云端历史
- **云端历史**：登录后对话历史存进 Supabase 的 `chat_history` 表，
  行级安全（RLS）按 `auth.uid()` 隔离 —— 每个账号只能读写自己的记录
- 未登录也能正常聊天，只是历史只留在本次页面里，关掉就没有
- 登录那一刻会把游客期间攒下的对话一起带上云端，不会白丢
- 退出登录清掉本机镜像（云端记录仍留在账号里）
- **可自建云端**：设置里新增「Supabase 服务器（高级，可选）」，
  可填自己的项目地址与可发布密钥，带「连接测试」和一份可折叠的建表 SQL
- 应用栏与设置里都有「退出登录」入口

### 修复

- 发送后待发图片缩略图一直挂在输入框上方（发送路径不重绘输入区）
- 从历史打开会丢掉 `sources` 与 `imageCount` —— 引用角标会变成死链
- 「整理成表 / 画个示意图」期间的「停止」按钮是假的
  （`jsonRequest` 的 AbortController 从没挂到 `state.controller`）
- 编辑态的用户气泡缺 `id`，定位很早的消息时会被甩到最底部
- 流式期间「更多」菜单被反复重建，打断正在悬停的菜单项
- 引用角标误吞正文里的序号（如「参考 [1] 号规范」）
- Key 校验遇 402 余额不足 / 429 限流时用了红字（Key 其实有效，改为中性色）
- `temperature` 与回答长度纳入快照；用量记账加会话守卫；历史用量过校验口
- `updateWebToggle` 的异常不再被静默吞掉

---

## [0.1.2] — 2026-09-24

### 修复

- **发消息后视口一路跑到最底端**：`scrollToMessageStart` 的 pin 会被自己
  触发的 `scroll` 事件撤销（`nearBottom()` 容差 120px 误判为贴底）。
  新增 `state.pinnedByCode`，只有用户真的动手（滚轮 / 触摸 / 拖动滚动条）才解除
- **新增设置「发出后视口」**：停在新回答的开头（默认）/ 一直跟到最新
- 点发送后画面先闪到底部再跳回回答开头（`settleKeyboard` 与 `rAF` 的顺序问题）
- 联网关键词提示永远不出现（`input` 里只改了状态、没刷新 DOM）
- 弹窗里 Key 输入框失焦后 `--kb` 可能残留（iOS）
- 位置保持式重绘会顺手把「贴底」状态解除

---

## [0.1.1] — 2026-09-24 · 手机体验

- 点发送后输入框悬在半空：`--kb` 只在 visualViewport 事件里更新，收键盘时不一定派发；加去重 + `settleKeyboard()` 多采样（0/60/140/260/420ms）
- 发完消息视口停在回答末尾：发送 / 重新生成后把视口钉在这条回答的开头（pin 即退出贴底），用户自己滚到底后恢复跟随
- 从历史进来总停在最上方：删掉 `keepPlace` 分支，现在统一落到最新一条消息的开头
- 「你还可以问」按钮占满整行、滑动误触：改 `align-items:flex-start` + `width:auto`，按钮宽度跟着文字走
- 点联网开关把阅读位置带走：改为原地更新按钮与提示，零重绘
- 新增 `renderKeepingScroll()`：开关面板、增删图片、整理 / 画图完成后精确还原滚动位置
- 回答长度改走快照，与推荐产品 / 思考强度一致
- `chatContent` 只有图时不塞空文本块

## [0.1.0] — 2026-09-24 · 体验版

- 修掉**发送键永久变灰**：`disabled` 只由模板写入、`updateControls` 从不清，任何一次 loading 期间的 render 都会把它永久留下；改为模板不写、每次显式复位
- 思维链：流式只显示「思考中 + 最近 3 行」，结束后收成一行横条「💭 思考了 8.4s · 展开」
- 联网检索状态条常驻回答上方：「🔍 正在检索…」→「🌐 已联网检索 5 条 · 2.3s」
- 「继续说完」：停止后按已写内容续写，续写指令只在当次请求里
- 快捷键：`Ctrl/Cmd + K` 设置、`Ctrl/Cmd + /` 速查、`Esc` 逐层退出、`↑/↓` 跳提问
- 图片缩略图点开看大图（纯前端灯箱）
- 导出与清空历史改用自家弹窗，不再用 `confirm`
- 空会话第一句话插进 `.chat-inner`（原来插到滚动容器外面看不见）
- 用量随历史存取，打开历史能看到那次会话的真实数字

## [0.0.9] — 2026-09-23 · 三栏固定布局

- 对话页改三栏：顶部常驻应用栏 + 中间独立滚动消息区 + 底部固定输入区
- 应用栏：返回 / 标题（空间 · 对象（话题））/ 副标题（状态 / 用量 / 档位 / 联网）/ 设置 / 更多菜单
- 消息区滚动条只在内部，顶栏与输入区始终可见
- 右下角浮动按钮：回到顶部 / 回到最新，离顶离底超一屏才出现
- 移除对话页大标题与面包屑；错误提示移到消息区顶部
- 用量从页面底部移到应用栏副标题
- 手机端弹窗改底部抽屉；历史搜索条吸顶
- 排版密度变量化：`--page-pad` / `--card-gap` / `--msg-gap` / `--bubble-pad`

## [0.0.8] — 2026-09-23 · 移动端与可验证

- 手机端重排：只压缩留白不缩小字号，卡片两列、气泡 96%
- 触屏 hover 复位；聚焦输入框不再被浏览器放大页面
- 字号可调（小 / 标准 / 大 / 特大），全站 rem + `--fs` 驱动
- 推荐产品联网版：允许品牌型号，但每条必须附真实来源链接，禁止编造 URL
- Markdown 链接渲染成可点网页
- 「整理成表 / 画示意图」不再静默失败，失败必提示、loading 必恢复
- 联网改走 Anthropic Messages 协议（`/anthropic/v1/messages` + `web_search_20250305`）
- 回答里 `[1] [2]` 渲染成可点角标，点了打开对应来源

## [0.0.7] — 2026-09-23 · 跟随 V4.1 Flash

- 模型名统一为 `deepseek-flash`，档位收敛为 4 个
- `payload.thinking` 由 `true` 改为 `{ type: "enabled" }`
- 新增 `reasoning_effort`：低 / 高 / 最高，默认 high
- 价格改为空闲 1 元 / 4 元（每百万 tokens），工作日高峰翻倍
- 新增「推荐产品」开关：只给类别、规格、价格区间，不给品牌型号
- 快照 `recommendProducts` / `reasoningEffort`，中途改设置不影响本条

## [0.0.6] — 2026-09-23 · 模型换代

- 模型档位制：快速 / 深度思考 / 旗舰 / 看图 / 自定义
- V4 混合思考：`thinking` 参数切思考，不再分两个模型名
- 联网搜索：输入区开关，走 `/responses` + `web_search`
- 思维链：解析 `reasoning_content`，可折叠「思考过程」块
- 图片输入：最多 4 张，压到 1600px 转 base64
- 「整理成表」：JSON 模式抽尺寸表 / 施工顺序 / 验收清单
- 「画个示意图」：JSON 模式取布局，前端 SVG 画平面图
- 用量统计：`stream_options.include_usage` + 本地按单价折算

## [0.0.5] — 2026-09-23 · 自查修复

- 重试流程不再凭空插一句「继续」污染对话
- reasoner 类模型不发 `temperature`，`max_tokens` 自动放大给思维链留额度
- HTTP 错误分类：401 / 402 / 429 / 5xx 各说各的
- 取消 / 停止后控件状态立即复位，不等下次渲染
- 清空历史同步清搜索框；未设 Key 的暂存问题覆盖前还原进输入框
- 历史搜索索引改 `id + updatedAt` 作 key，写库后仍命中
- 自动重试期间也落一次库，关页面不丢「问过什么」
- 流式结束后只重画最后一条气泡，长对话不整块重排
- 只认同源错误的全局兜底，插件报错不弹给用户

## [0.0.4] — 2026-09-23 · 设置与深色

- 深色模式（`prefers-color-scheme`）+ `prefers-reduced-motion` 降级
- 设置面板：模型、回答长度、temperature、空闲超时
- API Key 保存时校验有效性（区分无效 / 余额不足 / 无法验证）
- 单条回答重新生成；修改已发送的问题（改完自动重答）
- 导出 Markdown / 打印存 PDF；历史搜索 + 按空间筛选
- 回答末尾推荐 3 个追问，可直接点
- 条件新增「预算档位」；流式气泡尾部闪烁光标
- 键盘弹起用 `--kb` 顶起 composer / toast / 回底按钮
- 触屏 44px 触控区、`touch-action:manipulation`

## [0.0.3] — 2026-09-23 · 修 25 个问题

- 输入法回车不误发（`isComposing` / `keyCode 229`）
- 生成中按回车不吞字：先判断能发、再清空输入框
- 新增「停止生成」（AbortController）
- 中途离开的对话照样落库：用会话快照写历史，不看 `state.view`
- 流式离开页面不再索引越界：持有消息对象引用 + `abandoned` 标记
- 弹窗暂存问题用 `sessionId` + 时间戳校验，取消时还原回输入框
- 长对话按条数与字符预算裁剪，system 里告知省略条数
- 条件按空间分别保存，点 FAQ 不再清空已填尺寸
- 全站改事件委托 + `data-*`，无内联事件
- 表格判定放宽，流式中疑似表格行暂缓渲染
- `fetch` 空闲超时 45 秒，错误分类（超时 / 网络 / 服务端 / 空回复）

## [0.0.2] — 2026-09-23 · 改对话式

- 从「填表生成」改成「聊天追问」，`state.messages` 数组 + 全量历史发送
- 每个空间预置 6 条高频问题，点一下直接开问
- 条件表单砍到 2 项并折叠
- `SYSTEM_PROMPT` 改成四块结构：结论 / 为什么 / 对比表 / 买装坑
- 对话存 localStorage，首页可调回继续追问

## [0.0.1] — 2026-09-23 · 起点

- 三级导航：空间 → 设计对象 → 条件表单 → 生成建议
- 8 个空间、每个空间 6~10 个设计对象
- DeepSeek 流式输出，Markdown 轻量渲染
- API Key 存 localStorage，弹窗设置
