# 更新日志

本项目每次更新改了什么，都记在这里。版本号与页面右上角显示的 `vX.Y.Z` 一致。

---

## [0.2.7] — 2026-09-26 · 动效系统补齐，三处会让动画「崩一下」的硬伤一并修掉

### 🔴 P0：三个「点了之后页面不对劲」的硬伤

- **原地重绘会重放整页入场动画** —— `view.innerHTML = …` 每次都是全新元素，
  `#view > *` 的 `viewEnter` 就每次都会重播。所以在对话页点一下「🌐 联网」、
  删一张待发图片、切一下条件面板，**整个输入区都会重新上浮淡入一次**。
  现在 `renderKeepingScroll()` 期间置 `skipViewAnim`，切到 `body.no-view-anim`
  关掉入场动画，用 `try/finally` 保证复位
- **FLIP 配错了对象** —— 之前拿整个 `#view` 当动画容器，但整页重建后
  面包屑、tab 栏、排序折叠块、底部免责全变了，FLIP 会把这些**不相干**的元素
  按索引配对，动画乱飞。现在榜单条目包进 `<div id="rankList">`，
  切梯队 / 切权重只对这个容器做 FLIP
- **嵌套 `<details>` 展开时抖** —— 折叠块的高度动画原先是每个 `<details>`
  都包一层，而榜单条目内部又嵌了 `<details>`（设计决策、工具箱阶段），
  两层 `grid-template-rows` 一起算就抖。现在只有顶层做高度动画

### 🔴 P0：其余七条确定的 Bug

- **工具箱「预算分配」和工具箱里的独立工具重复** —— 删掉 `budgetSplitterHtml`
  整块（连同 `saveBudget`、`usingBuiltinCloud` 这些死代码）。预算分配本来就有
  独立入口，接进对话里是重复
- **填好 Key 之后引导卡还会再出现** —— `state.showKeyGuide` 只在 `stashPending()`
  里被置 `true`、从没置回 `false`，只要视图完整重绘一次它就复发
- **条件面板的展开状态刷新就丢** —— 新开独立键 `design_progress_open_v1`。
  它和 `progress` 的存储格式不一样，混进 `LS_PROGRESS` 要改现有格式并做迁移，
  风险太高，所以单独一个键
- **「新建会话」不先问就直接清空** —— 抽出 `confirmNewSession(then)`，
  有消息就先问、没消息直接执行，所有入口复用同一套判断
- **从榜单点「问 AI」时，正在填的条件被丢掉** —— `askRankItem` 改用项目自己的
  `askDialog`（返回 Promise），勾选保留时先跳会话再发消息，条件不丢
- **`goBack()` 自己维护了 `navPushed`** —— 和 `onHashChange` 重复计数，
  返回方向偶尔算错。现在只由 `onHashChange` 一处管
- **搜索跳进榜单后不知道点了哪一条** —— 目标条目闪 1.6 秒高亮再淡回

### 🟠 P1：动效系统（一整套 `:root` 变量）

- **时长与缓动统一成变量** —— 新增 `--dur-instant/fast/normal/slow/slower`
  和 `--ease-out/in-out/spring/soft`，外加一个统称 `--tx`。
  全局扫出 **23 处**硬编码时长（`.18s`、`.2s`、`.fifteen` 之类）全部换掉，
  现在 `transition: … .数字s` 的残留是 **0**
- **视图切换有过渡** —— `#view > *` 加 `viewEnter`（轻微上浮 + 淡入）
- **`.card` 漏了 `transform` 过渡** —— 补上，而且比其它属性稍长一点，上浮更有质感
- **折叠箭头从「换字符」改成 `transform: rotate()`** ——
  `.fold` / `.cond` / `.set-group` / `.think` / `.cond-more` 五处，终于能平滑转了
- **弹窗有了入场** —— 遮罩淡入，`.modal` 从 `scale(.96) translateY(8px)`
  用**弹性缓动**落位
- **消息、Toast、按钮、FAB 都有反馈** —— 新消息 `msgIn` 上浮淡入；
  Toast 改上滑 + 轻微缩小；`.btn`/`.tbtn` 按下 `scale(.97)`；FAB `fabIn` 弹入
- **后退时动画方向反过来** —— 按 hash 层级深浅判断方向，后退时新页从**左**边进来，
  才像「退了一层」而不是「又进一层」

### 🟡 P2：长任务不再只给三个跳动的点

- **骨架屏** —— 工具箱生成要 10~30 秒、联网检索要 20~40 秒，这段时间原来是
  空白 + 三个点，用户不知道要等多久、也不知道等来的是什么形状。现在换成
  「标题 + 两行 + 两张卡」的结构占位，带微光扫过的 `shimmer`，
  并标 `aria-hidden` 对读屏隐藏
- **顶栏滚动反馈** —— 往下滚过 8px 就加阴影、略微收矮，对所有页面生效
- **图片入场** —— `.thumb` / `.pending-img img` / `.tool-img img` 用 `imgPop`
  （`scale(.88)` + 弹性），不再「啪」地出现
- **列表错位入场** —— 榜单 / 历史 / 空间卡注入 `--i`，按 `calc(var(--i) * 40ms)`
  依次浮现。三处都用 `Math.min(i, 7)` **封顶**，避免条目多时「越等越久」
- **触觉反馈从 3 处扩到 7 处** —— 补上清缓存、底栏切页、开弹窗、复制成功

### 🟢 P3：细节

- **复制成功时按钮自己变 ✓** —— 四个复制入口点下去，按钮变成「✓ 已复制」，
  1.5 秒后变回。比只弹一个 toast 更像「这个动作成功了」
- **View Transitions 渐进增强** —— 支持的浏览器（Chrome 111+ / Safari 18+）
  会把重绘变成真正的过渡，不支持的同步执行、行为完全一样。
  加了防重入，并且和原地重绘联动（那种情况不走 VT，否则照样会闪）
- **名词表按空间分** —— 通用 12 条 + 10 个空间各 83 条，翻开就是本空间的说法
- **榜单扩充** —— 新增「全屋 · 不做不行」和「合同与付款」两张榜，
  梯队内加了 `rank-seq` 序号（先后顺序本身就是结论）
- **渲染窗口收紧** —— `msgRenderStart` 只渲染当前编辑位置前后的消息，
  长对话不再一次性铺满 DOM

### ⚪ 清理

- 删掉 `budgetSplitterHtml`、`saveBudget`、`usingBuiltinCloud` 及相关入口
- 删掉 `homeTab` 状态和 `case "starter"` 分支
- 页面离开时落盘 —— `pagehide` + `visibilitychange` 接上 `flushUpsertsOnExit()`，
  切后台不再丢未同步的记录
- 工具箱动作区补「清空这个工具的缓存」入口

---

## [0.2.6] — 2026-09-26 · 首页变成整屏封面，点标签就地展开，名词表按空间分

### 🔴 P0：点一个标签，正在看的页面没了

「XX里都包含什么」里点一个标签（比如厨房页点「橱柜」），原来走的是
`goObject()` → 只改 `objectId` 再弹一句「已切到 XX，继续聊就行」——
可用户人还在空间页，**根本没有可以聊的地方**，等于什么都没发生。

现在改成**就地展开对象面板**：`openObjectPanel()` 把面板插在标签墙下面，
列出跟这个对象相关的必做项（按对象名匹配，匹配不到就明说不硬凑），
底部给两个出口 —— 「看完整清单」和「**带着它问 AI**」。
只有真的点了后者，才会开会话跳过去。同时新增 `close-object-panel`（收起），
搜索里点「包含」那条也一并改成就地展开。

### 🔴 P0：其余四个必修

- **工具箱「📷 传图」点了没反应** —— 两个 `<input type="file">` 原来写在
  `viewChat()` 模板末尾，一切到工具箱就被 `view.innerHTML` 清掉，
  于是 `$("toolImgInput")` 恒为 `null`。现在提到静态 `<body>` 里
- **填好 Key 之后引导卡还会再出现** —— `state.showKeyGuide` 只在 `stashPending()`
  里被置 `true`、从没置回 `false`，只要视图完整重绘一次（点标签、切页面）它就复发。
  现在 `sendMessage()` 取到 Key 后立刻关掉，并在 `clearSessionState()` 里也重置
- **大结果进不了工具箱缓存，每次点都重新生成** —— 结果 JSON 超过 20k 字时
  `toolCacheSet` 存的是 `result: null` + summary，而命中判断是
  `hit.result || hit.summary`，这类结果的 summary 通常为空 → **永远命中不了**。
  现在加 `truncated` 标记一并认它，命中时提示「点重新生成可拿完整版」
- **手机聊天页输入框下方多出 64px 空白** —— 底栏在对话页是隐藏的，
  但 `padding-bottom: calc(var(--bottom-nav-h) + 8px)` 还在生效。
  现在补了 `body.chat-mode .composer` 覆盖

### 🟠 P1：首页 Hero 变成整屏封面

- **Hero 铺满首屏** —— 新增 `--appbar-h` 变量，`body.home-mode .hero` 去掉
  边距、圆角、边框、阴影，高度撑到「视口 − 顶栏」，内部按
  **眉标 / 主标题 / 标签+提示**三块用 `space-between` 铺开。
  左右内距用 `max(var(--page-pad), calc((100vw - 1400px) / 2 + var(--page-pad)))`，
  撑满的同时内容仍受 1400px 栏宽约束
- **三层背景** —— 暖色主光晕（右上）+ 冷色补光（左下）+ 底色渐变；
  叠一层 48px 细网格（用 `mask-image` 做中部显、边缘淡出）；
  再放一个 `min(80vw, 720px)` 的超大 R 水印当整屏底纹
- **「向上滑动」提示** —— 底部一个跳动的玻璃圆箭头 + 一行小字，
  滚过一屏 20% 后淡出；它走的是**文档流**（`position: static`），
  不会和固定的底部导航打架
- **内容依次浮现** —— 首页四块用 `IntersectionObserver` 做 `reveal`，
  滚进视口才上浮淡入，延迟 0 / 80 / 160 / 220ms 错峰；
  引导卡多一层暖色投影。带 `prefers-reduced-motion` 与不支持 IO 时的兜底
- **首页历史折成一行胶囊** —— 原来 Hero + 引导卡 + 搜索 + 示例叠起来
  把历史推到 700px 开外。现在折成「💬 最近问过的 · N 条　展开 ▾」一行，
  点一下原位展开完整列表

### 🟡 P2：入口合并与内容扩充

- **底栏「房间 / 问 AI」合并成一个入口** —— 两个入口原来进的是**同一个页面**、
  只有标题不同，8 个房间格子一模一样。现在底栏只留 **首页 / 房间 / 工具** 三项，
  房间页顶部加「看必做清单 / 直接问 AI」两个 tab 切换
- **全屋重做成「不做不行」14 条底线** —— 按
  **水与电 / 燃气排烟与封窗 / 埋进去就改不了的** 三张榜分组
  （电路分路、卫生间等电位、水管打压、排水坡度、防水闭水、燃气管道、
  烟道止逆阀、排风止逆阀、封阳台、验收时间线、空调孔与排水、隔音、
  网线预埋、地面找平）
- **新增「合同和钱」板块 12 条** —— 按 **开工前 / 施工中 / 出了问题** 分组，
  从预算分配、选公司、合同附件、付款节点，到工期排序、材料进场、成品保护、
  留证方法，再到增项谈判、质量整改、投诉路径、尾款保修
- **名词速查拆成两张表** —— `GLOSSARY_COMMON`（12 条通用）+
  `GLOSSARY_BY_SPACE`（10 个空间各自的词，共 **83 条**），
  专属在前通用在后、同名以专属为准；每条按
  「这是什么东西 → 干什么用 → 关键数字 / 做法 → 不做会怎样」的顺序写
- **首页引导卡三步改四步** —— 全屋底线 → 房间细节 → **合同和钱** → 问 AI；
  hero 标签同步更新

### 🟢 P3：一堆细节

- **榜单不再默认展开第一条** —— 原来用 `isFirst` 强开，一进榜单就被一条
  展开的内容顶下去。现在一律收起，只有「从搜索定位某一条」时才自动展开
- **「关于 RoomWise · 数据与隐私」从首页移进设置** —— 首页只留一句人话；
  设置里新增一组，内容带版本号，用 `renderAboutInSettings()` 在打开弹窗时填充
- **首页示例区删掉** —— 「不知道问什么？试试：」和三个示例问题一并移除
- **`.wrap` 底部留白统一** —— 基础 64px、≤640px 与 ≤480px 都走
  `calc(var(--bottom-nav-nh) + 48px)`（实际 104px），不会再出现
  480px 上下切换时底部忽宽忽窄
- **「问 AI」缺 Key 时给醒目横幅** —— 选房间页顶部说明「约 3 分钟能搞定」+
  一个「现在去填 ›」按钮，看清单模式不显示
- **名词速查词条名放宽到 6.5em** —— 否则「3C 钢化玻璃」「等电位联结」会被挤成两行
- **`rankItemHtml` 不涉及材料费时不显示「¥0~0 元」** —— 改显示说明文字
- 另外还修了：顶栏标题在工具页显示成「厨房」、返回上一级清空对话、
  从搜索点进清单 tab 总是不对、同一条记录每次进榜单都自己弹开、
  「先看必做清单（免费）」点了不动、设置面板找不到 Key 输入框、
  条件面板每次自动展开、单空间预算档位用错量级、S 级标签文案、
  榜单筛选一次铺十几个按钮、`openModal` 对隐藏元素调 `focus`、
  关闭联网弹窗后请求还在跑、`guiToRank` 找已删元素等问题

### ⚪ 清理

- 删掉一批没人引用的死代码与死样式：`wholeBannerHtml()`、`homeSpaceCards()`、
  `toolsHtml()`、`viewDesign()`、`wholeTip`、`starterHtml`/`STARTERS`、
  `.about-fold`、`.starter-*`、`.rank-mini-grid` 等
- 品牌名统一为 RoomWise：导出图片水印、导出文件名、导出 Markdown 标题与页脚

---

## [0.2.5] — 2026-09-26 · 能返回了、看得懂了、找得到 Key 了

这一版是照着「28 条体检清单」逐条改出来的，外加一轮实测发现的 12 个具体 bug。
一句话概括：**手机返回键终于不会直接退出应用**，**每一页都知道自己在第几步**，
**小白第一次打开也看得懂要干什么**。

### 🔴 P0：会迷路 / 标题错 / 返回退出应用

- **接上浏览器历史（hash 路由）** —— 原来完全没有路由，手机侧滑返回、浏览器后退键会
  **直接退出应用**，页面也不能分享、收藏、新标签打开，刷新还回首页丢上下文。
  现在每个视图都有自己的地址：`#/home`、`#/space/kitchen`、`#/rank/kitchen/soft`、
  `#/toolbox`、`#/tool/traps`、`#/chat/living`…，监听 `hashchange`。
  刷新能恢复当前页，链接能分享，**手机返回键和页面上的「←」是同一个动作**
  （靠一个 `navPushed` 计数判断历史栈里有没有自己压进去的条目，直接打开链接进来时才走层级兜底）。
  非法地址有兜底，不会白屏
- **桌面端补上主导航** —— 底部导航只在 ≤640px 显示，桌面顶栏原来只有 🧰 👤 ⚙，
  没有「首页 / 房间 / 工具箱 / 问 AI」。现在 >640px 时顶栏出现一组文字导航，当前页高亮；
  和底部导航**共用同一组 `data-act`**，点击逻辑一行没重写
- **顶栏标题两个 bug** —— 原来的顺序是先读 `state.space` 再判 `toolbox`，
  导致首页点 🧰 显示「室内设计助手」；而 `traps`/`talk`/`ask`/`design`/`budget` 这些工具页
  根本没有分支，会掉进通用逻辑显示成「厨房」。现在**先判工具箱和工具页，再读 space**
- **返回上一级不再清空当前对话** —— `goHome()` / `goSpace()` 原来都无条件调
  `clearSessionState()`，聊到一半点返回，对话直接从页面消失。现在这两处不清会话
  （对话跟 session 走，不跟页面视图绑），`clearSessionState()` 的调用点从 5 处减到 2 处
- **补上 `.tbtn.primary`** —— 这个 class 用在「✨ AI 生成」「■ 停止」和引导主按钮三处，
  却**没有任何样式定义**，看起来和次要按钮一模一样，用户不知道哪个才是「点这个」

### 🟠 术语统一与入口区分

- **不再叫「排行榜」** —— 全站统一成「**必做清单**」：榜单页标题、没 Key 时的引导卡、
  引导按钮、代码注释都改了。原来同一个东西在首页叫「清单」、在没 Key 的页面叫「排行榜」、
  在页面标题里叫「最需要先解决的排行榜」，同页还混着「排名」
- **两个入口说清楚了** —— 「房间」和「问 AI」原来进的是同一个页面、只有 `pickMode` 不同，
  用户分不清。现在标题分别是「**按房间看必做清单**」和「**直接问 AI**」，
  说明里补了「不用联网、不用填 Key」和「全屋级的事（预算、合同、增项）选全屋就行」
- **误导性文案** —— 「问设计师」→「**问 AI**」（实际就是调 AI 对话）；
  「联网搜索」→「**搜别人家怎么做的**」；「先看排行榜」→「**先看必做清单**」

### 🟡 新手引导：每一页都知道自己在哪

- **全局步骤徽章** —— 面包屑右侧显示「第 2 步 / 共 3 步 · 挑房间」，
  和首页那张三步引导卡是同一套说法。进了房间之后文案会变成「看这个房间的必做清单」，
  不再说「挑房间」（人都进来了还说挑房间就错了）
- **条件面板不再一次抛 13 格** —— 默认只展开**面积 / 房屋类型 / 常住人口 / 预算** 4 项，
  其余 9 项收进「填得更准（9 项，可选）」折叠
- **首页三个一键示例** —— 「3 万块钱先做厨房还是卫生间？」「工头说这个不用做，是真的吗」
  「卫生间防水要做几遍才够」，点了直接进全屋对话并把问题发出去，第一次打开的人 10 秒内看到价值
- **榜单筛选减负** —— 四个预设收进「排序方式 · 切换只改排序，不重新请求」折叠，
  展开后解释四种权重各偏重什么
- **装修名词速查** —— 空间页新增折叠块，12 个术语各一句人话解释：
  打压试验 / 闭水试验 / 点位表 / 空鼓 / 找平 / 增项 / 隐蔽工程 / 动线 / E0 级 /
  返工 / 门槛石 / 存水弯
- **AI 回答下方常驻风险提示** —— 「仅供参考，不构成施工依据；涉及承重、燃气、电路的改动，
  请找专业人员确认。」原来这句话只出现在清单页和导出图里，对话里反而漏了
- **首页技术小字收进折叠** —— 「思维链横条 · Supabase · API Key…」这些原来铺在首页最底部、
  最小最灰，现在收进「关于 RoomWise · 数据与隐私」，首页只留一句人话
- **设置面板分组** —— 「连接」→「连接（填 API Key）」、「模型与回答」→「模型与回答（技术项）」，
  默认展开「连接」和「界面与偏好」两组（最常用的），技术组收起
- **条目摘要更实用** —— 榜单单条目**收起状态**就直接显示「🔑 关键数字」，
  不展开也知道「做到什么程度算对」

### 🎨 视觉与可读性

- **「房间 / 问 AI」选择页改成一行两个** —— 从 3 列改成 **2 列**，卡片明显放大
  （图标 52×52、房间名 1.0625rem、内距 26px），手机上接近半屏宽但四周和中间都留了间距
- **对比度达标** —— 承载解释文字的色阶压深：浅色 `--text-3` `#8a8578` → **`#6d6857`**
  （3.7:1 → 约 5:1），`--text-4`/`--text-5` 同步加深，深色主题两处定义都改了
- **默认字号抬到 1.05** —— 原来 12/13px 的小字偏小，留白不动
- **hero-tags 对读屏可见** —— 去掉 `aria-hidden`，三个能力胶囊读屏也能听到
- **删除死代码** —— `wholeBannerHtml()` / `homeSpaceCards()` / `toolsHtml()` /
  `viewDesign()` / `wholeTip` / `state.histShowAll`，以及 `.guide-strip` / `.gs-*` /
  `.whole-banner` / `.wb-*` / `.rank-mini-grid` 等只有样式没有 JS 的死规则

### 🐛 实测发现的 12 个具体 bug

- **「查看全部 N 条」点了没反应** —— `hist-all` 设的是 `state.histShowAll`，
  但渲染判的是 `state.histExpanded`，两个变量名对不上
- **从搜索点进清单 tab 总是错的** —— `openRankToItem` 先设了 `rankTab`，
  紧接着 `openRank` 内部又写死 `"hidden"` 把它覆盖回去
- **`state.rankFocus` 从不清空** —— 同一条记录每次进榜单都自己弹开。
  现在 `openRank()` 开头清掉，只有从搜索定位时才设
- **「先看必做清单（免费）」点了不动** —— `guiToRank()` 找的 `.rank-mini-grid` 早就删了，
  `g` 是 `null` 什么都不滚。现在直接进全屋房间主页；
  同一个坑还发生在 `case "guide-space"`，一并改掉
- **`hs-rank` 没传 name** —— 从搜索点进榜单本该自动展开搜到的那条，因为 `data-name` 没存而展开不了
- **四处引导文案** —— 空对话的 placeholder 写着「继续追问」（第一次对话没有上文可以「继续」）；
  搜索框提示补「也能搜房间名」；步骤条文案分场景；设置面板默认展开项调整

> **本版已知未做（留到下个版本）**：游客历史落到 localStorage（现在未登录只留内存，关掉就没）、
> 云端建表 SQL 从首页收进高级设置、引导卡第 01 步的走向（现在是进全屋房间主页，榜单作为页面内入口）。

---

## [0.2.4] — 2026-09-26 · 改名 RoomWise，首页重做，修「模型没有返回内容」

这一版是三件事凑在一起：**换个说得清楚的名字**、**把首页从「一眼看不懂」改成「跟着走就行」**、
**修掉一个会让回答直接失败的硬伤**。加起来动了 60 多处，以下是分块的说明。

### 🔴 P0：AI 提问时「等很久，然后说模型没有返回内容」

用户实际撞上的问题：问一句，等很久，最后只回一句「生成中断 · 模型没有返回任何内容」。
查下来是三件事叠在一起：

- **`empty` 不在自动重试名单里** —— 原来的可重试类型只有 `net` / `timeout`。
  而「服务端把响应正常收尾了却没吐正文」属于 `empty`，它直接报错、不补发。
  这跟网络抖动本质是一回事，现在把 `empty` 也纳入可重试
- **空闲计时从发请求那刻就按 45 秒倒数** —— 联网搜索要等服务端检索、思考模式要先算完一轮，
  这段时间服务端**一个字都不吐**，45 秒直接被判死。现在拆成两档：
  **首字节前**给更长宽容期（联网 150 秒 / 普通 100 秒），**看到第一个字之后**才收紧回用户设的 45 秒
- **`empty` 时只要正文为空就把整条消息删掉** —— 哪怕模型已经输出了思考过程也照样删，
  用户什么都看不到。现在改成「只要有思考内容就留着」，并把提示写清楚：
  有思考 → 「模型只输出了思考过程，没有给出正文」；纯空 → 「模型这次没有返回内容（服务端可能繁忙）」，
  两种都补一句「再点一次发送通常就好」
- 重试时一并清掉 `reasoning` / `thinkStart` / `thinkMs`，否则重试那轮的思考会接在上一轮后面越堆越长

### 🔵 改名：室内设计助手 → RoomWise

原来那个名字看不出是干什么的，页面标题、应用栏、Hero、版本号全部统一：

- `<title>` / `BASE_TITLE` → **RoomWise · 装修小白的第一站**
- 首页 Hero → **RoomWise**，配一枚 R 徽标
- 控制台日志前缀 `[室内设计助手]` → `[RoomWise]`
- 版本号去掉「副本」后缀，正式文件名回归 `室内设计助手0.2.4.html`

### 🟠 「清单」改名「房间」，点进去先看房间主页

之前底栏第一格叫「清单」，点进去直接打开那个空间的排行榜 —— 跳过了中间一层。
现在：

- 底栏 **🏠 首页 · 🚪 房间 · 🧰 工具 · 💬 问 AI**
- 点「房间」→ 「**进哪个房间？**」九宫格 → 点某个房间 → 进的是**房间主页**，
  最上面才是「最先看这个（N 条）」的榜单入口
- 首页引导卡第 1 步「**先定全屋的调子**」也改成进**全屋房间主页**，不再直跳榜单
- 全站已经没有「默认跳到榜单」的入口了，`openRank` 只在用户主动点入口时才调用
- `appbarTitle` / 面包屑 / 搜索标签的文案同步改成「房间」

### 🟡 首页重做：从「一堆卡片」改成「三步走」

原来首页铺着全屋大卡 + 九宫格 + 历史，信息密度高但看不出先后顺序。现在：

- **三步引导卡**（顶部）：`01 先看全屋清单` → `02 再挑房间看细节` → `03 拿不准就问 AI`，
  三步之间是分隔线列表，第 1 步带暖色高亮，hover 时序号变色、箭头右移
- **搜索框**下移到引导卡之后
- 去掉了全屋大卡（它的职责被引导卡第 1 步接走），首页顺序变成：
  **引导卡 → 搜索框 → 最近问过的**
- 「最近问过的」加了 **💬 图标徽章 + 右侧条数角标**，每条历史前面加了**空间图标块**
  （厨房 🍳 / 卫生间 🚿 / 卧室 🛏️），问题文字两行截断
- **Hero 只在首页显示** —— 房间 / 工具 / 问 AI / 空间 / 榜单 / 对话页都收起，
  内容直接顶上来，不再被一块大 logo 占掉首屏

### 🎨 设计系统与视觉统一

- **底栏改毛玻璃**（`backdrop-filter: blur(14px)`），和顶栏语言统一；
  图标套上 36×28 圆角容器，选中时容器填暖色 + 内描边 + 图标轻微上浮放大
- **卡片语言统一**：全部改成 1px 细边框 + 大圆角 + 去掉重投影，hover 只变边框和淡阴影、不再位移
- **Hero 做成品牌卡片**：暖色渐变底 + 右上角柔和光晕 + 右下角点阵装饰，
  logo 带高光，slogan 前加一条短横线；桌面 52px 内距 / 2.375rem 标题，
  1280px 升到 60px 内距 / 2.75rem，手机上收到 36px / 1.875rem
- **Slogan 下面加三个小胶囊**：`🏆 13 条全屋清单` `🍳 8 个房间` `✨ 不用填 Key 也能看`
- **`.crumb:empty` 不占位** —— 首页没有面包屑，不再用 `min-height` + `margin` 空出一大块
- 字号 / 间距 / 圆角 / 阴影抽成 CSS 变量（`--sp-*` / `--radius-*` / `--shadow-*` / `--fs-*`），
  全站中文优先系统字体，**不引入任何外部字体或 CDN**

### 🟢 设计决策加「这是什么」

「设计决策」那 53 条原来只有「为什么做 / 代价 / 什么情况别做」，小白看到「干湿分离」「高低台」
这种词根本不知道指的是什么。现在每条前面补一行**「这是什么」**（一句大白话解释术语），
折叠状态下的摘要也优先显示这一行。老数据没填 `what` 时自动退回显示 `why`，不会空着。

- 覆盖 **9 个空间共 53 条**：厨房 6 / 卫生间 6 / 客厅 5 / 卧室 5 / 餐厅 4 / 书房 5 / 玄关 5 / 阳台 5 / 全屋 12

---

## [0.2.3] — 2026-09-25 · 修好工具箱联网必挂的 JSON 解析

### 🔴 P0：工具箱联网必挂「返回的不是合法 JSON」

这一条是用户最直接撞上的问题。按四个方向一起修：

- **输出额度被输入挤爆** —— 之前检索结果塞 6000 字，而输出额度只有 4000，
  结构化输出写到一半就被 `finish_reason=length` 掐断，JSON 尾部缺失。
  现在：检索结果收紧到 **2500 字**，输出额度提到 **8000**，思考模式下**再翻倍**
- **提示词加「额度纪律」** —— 明确要求「装不下时优先保证 JSON 结构完整，
  宁可精简字段内容，也不要让 JSON 被截断。所有数组和括号都必须闭合」
- **`parseJsonLoose` 加两级修复** —— 新增 `repairTruncatedJson()`：
  按「最后一个完整的 `}` 或 `]`」截回去、补上未闭合的字符串与括号。
  顺序也调整了：**修复版要排在「最后一个 `{` 到最后一个 `}`」之前**，
  否则截断的 JSON 会先被切出一个内层小对象，拿到残缺结果
- **解析失败自动重试一次** —— 第二次用精简 prompt（检索结果压到 1000 字），
  仍失败才降级成纯文本展示
- **思考模式不再发 `response_format`** —— 部分网关在「思考 + JSON 模式」下会把
  思维链混进 content，干脆退回纯提示词约束

### 🟠 P0：其余三个功能性 BUG

- **`stashPending()` 有一段永远跑不到的死代码** —— `return true` 写在
  `pending = {...}` 之前，导致「填完 Key 后自动发刚才那条」永远失效。
  现在 pending 的写入挪到了最前面
- **切工具时 `extra` 残留** —— `openTool()` 和 `viewToolWorkbench()` 两处逻辑不一致，
  从「报价单陷阱」切到「设计决策」时前者的字段会带进后者。现在统一到 `openTool()`
- **工具箱缓存键漏了自定义模型名** —— 原来用 `settings.modelKey`，
  选「自定义」档位时改 `customModel` 缓存键不变，会命中旧模型的旧结果。
  现在改用 `currentModel()`（实际发出的模型名）

### 🟡 P1：数据缺口与缺失功能

- **全屋的设计决策原来是空的** —— `DESIGN_DECISIONS` 没有 `whole` 键，
  全屋页那块是空的。现在补了 **12 项**：全屋智能、中央空调、地暖、新风、全屋净水、
  全屋 Wi-Fi、地板通铺、无主灯、全屋定制 vs 成品、拆改格局、开放式布局、干湿分离
- **API Key 旁边的「?」教学弹窗** —— 设置里的 Key 输入框后面加了一个小问号
  （比密码框的小眼睛小一圈），点开是五步取 Key 的说明 + 提醒 + 直达链接
- **工具箱的来源约束补上了** —— 提示词里明确「来源链接只能引用本次检索结果里
  实际出现过的 URL」；前端再加一层**白名单**：模型自己写在 `source` 里的链接
  不在白名单内，一律不渲染成可点链接

### 🟢 P2：健壮性与一致性

- **工具箱和聊天不再共用一个用量池** —— 新增 `state.toolUsage`，
  `recordUsage` 支持 `{ from: "tool" }`，跑工具箱不会再让聊天的 token 计数乱涨
- **`recordUsage` 在工具箱路径下补了会话守卫** —— 之前第三个参数是空的，
  切会话后残留的 chunk 会把 token 记到新会话头上
- **去掉「结尾给 3 个可追问问题」的要求** —— schema 里没有对应字段，
  模型只能混进别的字符串里。工具箱结果页本来就有「问设计师 / 重新生成」等动作
- **设计决策的结论判定放宽** —— 原来只认「不建议」「有条件」，模型说「不适合」
  「看情况」都会掉到灰色。现在用多关键词覆盖：不建议 / 不适合 / 不推荐 / 别做 / 不要做；
  有条件 / 看情况 / 视情况 / 取决于 / 不一定
- **`webUnsupported` 不再一次锁死** —— 之前某次联网端点 404 后就永久置 true，
  之后手动打开「🌐 联网」也不走联网分支。现在切换会话时重置
- **进度勾选后计数立刻更新** —— 之前要等下一次整页重绘
- **联网案例的分段方式改了** —— 原来按空行切，模型在案例摘要里换行就会被劈成两条；
  现在按「链接出现的位置」分界

### ⚪ P3：清理

- 删掉没有引用的死代码：`TOOL_JSON_RULE`、`TOOL_TITLES`、`toolFallbackData`、
  `viewTraps / viewTalk / viewAsk` 三个包装函数
- `rankCheckHtml` 的双格式兼容先保留（和排行榜改动一起做更省事）

### 说明

- 网页现在 **615 KB**（0.2.2 是 604 KB）
- 仍是单文件、无外部依赖

---
## [0.2.2] — 2026-09-25 · 首页只留两步，工具箱搬到顶栏

### 🏠 首页重构：从「一堵墙」变回「两条路」

- **删掉首页的排行榜区块** —— 首页不再铺 9 张小卡，改由「第一步」那张大卡承担
- **删掉首页的工具箱区块** —— 工具箱搬到顶栏，首页少一堵卡片墙
- **重写引导为两步**：
  - **第一步：先看全屋** —— 一张醒目大卡（金色边框），直接进「全屋」空间
    （钱怎么分 / 合同怎么签 / 工期怎么排）
  - **第二步：选一个空间** —— 8 个空间卡（**已排除全屋**，避免和第一步重复）
- **hero 文案**改成「先看全屋，再看空间。有问题随时问」
- **底部说明**改成「排行榜内容完全离线、不需要填 Key；工具箱请点右上角 🧰」

### 🧰 顶栏新增工具箱入口

- 顶栏加 🧰 按钮（顺序：🧰 → 👤 → ⚙ → ⋯），点开是**独立的工具箱列表页**
- 新增 `viewToolbox()` 列表页，包含全部 6 个工具
- 面包屑、标题、Esc / 返回上级都支持：**从工具箱进的具体工具，返回时回到工具箱列表**
- 手机上自动隐藏版本号（`v0.2.2`），给按钮让出位置

### 💰 工具箱新增「预算分配」（第 5 个 AI 工具）

- 输入总预算 + 面积户型 + 房屋类型 + 特殊需求，AI 给出**三块 → 分项**的分配方案：
  - 每块给比例与金额，每块下面再列分项（水电、防水、瓷砖、木作…）
  - 每块可以带一条提醒（哪些不能省）
  - 另给「重要提醒」「可以后补的项」和「针对你的情况」的调整建议
- 金额要求能加得起来对得上总预算
- 没填 Key / 失败时显示**通用分配参考**（隐藏 30~35% / 表面 40% / 软装 25~30% + 机动金提醒）
- 复制、导出、问设计师、存图等操作与其他工具一致

### 🔀 全屋页移除预算滑块

- 全屋榜顶部的**预算滑块已下线**（功能移到工具箱的 AI 预算分配）
- 全屋页改为一句提示：「预算分配已移到工具箱（点右上角 🧰 → 预算分配）」

### 说明

- 网页现在 **604 KB**（0.2.1 是 592 KB）
- 仍是单文件、无外部依赖
- 工具总数：报价单陷阱 / 工头话术 / 该问什么 / 设计决策 / 预算分配 / 我家进度

---
## [0.2.1] — 2026-09-25 · 让新手走得进来，让内容用得上

### 🚪 P0：新用户路径（不然根本走不到核心）

- **首页加「第一次装修？按这个顺序来」三步引导条** —— ① 全屋·钱该怎么花 →
  ② 看你家的空间 → ③ 有问题随时问，每步都可点
- **排行榜升为一级入口** —— 首页新增「🏆 先看这个」区块，9 个空间各一张小卡
  （显示条数与一票否决数量），不用再「点空间 → 找卡片 → 进榜」绕三层
- **没填 Key 的转化路径补齐**：
  - 首页底部明确写「没填 Key 也能用：排行榜和工具箱的内置清单完全离线可用」
  - 没 Key 点提问时**不再直接弹设置**，而是在消息区给一张引导卡：
    「先看排行榜（免费）」/「去填 Key 开始提问」/「先不用」，并附三步取 Key 的说明
- **全局「我家档案」** —— 面积、层高、人口、老人小孩这些和空间无关的条件，
  填一次所有空间共用；条件面板顶部有「与我家档案同步」开关，存本机、跨会话保留

### 🧭 P1：从「工具」走向「科普」

- **新增「全屋榜」（13 条）** —— 新手最痛的那一块：预算总盘、公司筛选、合同条款、
  付款节点、隐蔽工程验收时间线、工期排布、材料进场、成品保护、增项谈判、返工流程、
  纠纷处理、尾款保修。**全站条目 102 → 115 条**
- **预算分配器** —— 全屋榜顶部：输入总预算，拖动隐藏/表面/软装的百分比，
  实时算出各项金额（默认 35/40/25，参考行业经验），存本机
- **「我家进度」时间轴（新工具箱入口）** —— 把全站 115 条按 `check.when` 自动归成
  开工前 / 水电 / 防水 / 瓦工 / 木工 / 油漆 / 收尾 七个阶段，做完勾一个；
  勾选状态存本机，刷新、关掉再打开都还在
- **结果存图** —— 每条回答多一个「存图」按钮，把文字渲染成 750px 宽的长图
  （白底黑字 + 底部水印），方便发给伴侣、父母
- **AI 回答加「本条依据」块** —— 系统提示词要求结尾单独输出一行「依据：…」，
  前端摘出来渲染成独立小卡；同时明确要求**不编造规范号**，不确定就写「具体编号请自行核对」
- **错误提示全部改成口语** —— 去掉「CORS」「限流」「服务端异常」这类术语：
  「公司网络或浏览器插件可能拦住了请求 —— 换个网络，或关掉广告拦截插件再试」

### 🔧 P2：工程问题（不影响普通用户，但会咬人）

- **历史搜索缓存键改了** —— 原来注释写「id + updatedAt 当 key」，但 updatedAt 一变 key 就变，
  缓存必然失效；现在改成 id 做 key、updatedAt 存进 value 当版本号
- **流式输出不再全量重解析 Markdown** —— 原来每来一个 chunk 就重解析整段，整条回答是 O(n²)；
  现在流式期间只做转义 + 换行，结束后再完整渲染一次（表格在生成完时出现）
- **PNG 透明区域不再变黑** —— 压缩成 JPEG 之前先铺一层白底
- **安全项一票否决** —— safety 或 rework ≥ 9 的条目**强制进 S 级**，条目上标「一票否决」，
  不再被加权平均稀释（等电位联结这类不能被其他维度拉下去）
- **云端写入加 1.5 秒防抖** —— 连续编辑同一条只发最后一次
- **RANK_DATA 改成 `JSON.parse`** —— 不再让首屏解析几百 KB 的 JS 字面量

### 说明

- 网页现在 **592 KB**（0.2.0 是 545 KB）
- 仍是单文件、无外部依赖

---
## [0.2.0] — 2026-09-25 · 工具箱全面 AI 化

### 🎯 工具箱从「静态清单」升级成「AI 工作台」

原来那四个工具（报价单陷阱 / 工头话术 / 该问什么 / 设计决策）是**给你看**的清单，
现在它们变成**按你的情况生成**的工作台：可以填文字、传截图、联网核对，
生成结构化结论。

每个工具现在都能：

- **填自己的情况** —— 各自有专用补充字段（报价总额 / 付款节点 / 阶段 / 面积 / 尺寸层高 / 人口习惯…）
- **传截图** —— 最多 4 张，自动压缩；用**独立的图片区**，不跟聊天混
- **联网**（默认开）—— **先检索真实资料，再结合你的情况分析**，结果附可点的来源链接
- **生成结构化结果** —— 每条带等级 / 原文 / 问题 / 后果 / 应对话术 / 合同条款建议
- **一键处理** —— 复制全部 / 导出 Markdown / 问设计师 / 重新生成 / 保存到当前对话
- **过程中可控** —— 有停止按钮、失败可重试、结果按内容缓存

原来的静态清单**没有删**，现在是**兜底数据**：无 Key / 断网 / 接口失败时才显示，
并在顶部黄条明确标注「**这是内置通用参考，不是根据你的情况生成**」。

### ✅ 新增

- **灵活度扩展到 14 档 + 自定义**：0.0 严格 → 2.0 最大，也可以直接填任意值；
  非法值（空、非数字、超出 0~2）一律回落 0.7；思考模式下会提示不生效
- **工具箱可临时覆盖灵活度** —— 只影响本次生成，不动全局设置
- **结果缓存** —— 同参数直接读上次结果（刷新后仍在），「重新生成」忽略缓存；
  每个工具最多 20 条、合计 80 条，超限淘汰最旧；单条超过 20k 字只留摘要
- **来源只认真实检索结果** —— URL 只从联网返回里收集，从根上堵住模型编链接；
  没搜到就明确写「本次没有检索到可验证来源」
- **工具箱与聊天完全隔离** —— 用独立的 `state.toolController`，互不干扰；
  生成中按 **Esc** 先停止生成，**Ctrl/Cmd + Enter** 直接生成
- **提示词底线统一** —— 不编 URL / 价格 / 规范编号；只为用户输入与检索结果负责；
  涉及承重、燃气、电气、防水必须提醒找专业单位；信息不足先给通用判断、只问一个关键问题

### 说明

- 网页现在 **545 KB**（0.1.9 是 487 KB）
- 仍是单文件、无外部依赖、无 CDN

---
## [0.1.9] — 2026-09-25 · 修掉三个会出错的问题，加上工具箱

### 🔧 修复：三个会直接出错的问题

1. **排行榜索引错位** —— 点「问设计师 / 联网搜索」会跳到**错误的条目**。
   `viewRank()` 按 S / A / B / C 梯队分组渲染，每组索引都从 0 重新开始，
   但按钮用的是扁平数组序号：S 级有 2 条时，A 级第 1 条传的是 0，
   实际取到 S 级第 1 条。现在改成统一的扁平计数器。
2. **复制条目出现 `undefined`** —— `copyRankItem` 引用了不存在的 `it.basis`，
   复制出来是「依据：undefined（…）」。已删除，并去掉重复的「依据」行。
3. **Markdown 链接可注入** —— 链接正则不排除引号：模型只要输出
   `[x](https://a.com/"onmouseover="alert(1))`，就能把事件处理器塞进 `href`。
   现在 URL 一律走属性转义。

### 🔧 修复：逻辑与清理

- **设置改动先落地再校验** —— `saveSettings()` 原来一上来就改 `settings.*`，
  Key 无效时也会静默改掉设置，还会因为提前 `applySbConfig()`
  触发「云端项目已切换」把人登出。现在先校验 Key，通过后才写入。
- **图片细节没走快照** —— `chatContent` 读的是实时 `settings.imageDetail`，
  其他参数都走快照，唯独它漏了，生成途中改设置会串进正在跑的请求。已一并快照。
- 删除死代码：`extractTextBlocks()`、`state.lastJsonError`、`state.lastJsonChannelFail`；
  `hostOf()` 与 `shortHost()` 功能重复，统一用更稳的 `shortHost()`。
- 文件头注释里的版本号（还写着 v0.1.4）已更正。

### ✅ 增加：证据等级（零新增位置，权威度提升最明显）

- 排行榜每条摘要行多一个小角标：**国标强制** / **行业共识** / **看需求**
- 这是**事实标签，不是评分** —— 刻意不做「4.8 分」「五星推荐」
- 102 条全部标注：国标强制 **14** 条、行业共识 **41** 条、看需求 **47** 条

### ✅ 增加：验收动作卡（把「怎么验收」拆成照着做的五步）

每条把原来一句话的「怎么验收」升级成五行：

```
怎么验收
├ 什么时候   防水层固化 24 小时后
├ 拍什么     防水层照片（卷尺入镜看高度）+ 闭水水位线
├ 看什么     48 小时后楼下顶面、水位线是否下降
├ 不合格     水位下降 / 楼下渗水 / 防水层起泡
└ 怎么办     立即停工，不贴砖，要求重刷并重新闭水
```

102 条全部重写；旧数据如果是字符串写法也能正常显示。

### ✅ 增加：工具箱（首页新增，4 宫格小卡片）

刻意用**小卡片**（`card mini`），和数据里的空间区分开 ——
它不是第五个空间，不要让人以为可以「进入」。

- **📋 报价单陷阱** —— 6 类常见写法，每条给「后面会怎样」+「怎么应对」
- **🗣 工头话术** —— 12 句常听到的话，给「大概率意思」+「你可以这么接」
- **❓ 该问什么** —— 按 5 个阶段列好的问题，每个都写了「为什么问」
- **⚖️ 设计决策** —— 8 个空间共 **41** 项设计手法

### ✅ 增加：空间页的「设计决策」

位置在排行榜之后、具体问题之前，和排行榜形成两条腿：
**施工该做什么** + **设计该做什么**。
每项写清 **为什么做 / 代价 / 什么情况别做** —— 该不该做取决于户型和生活方式，
不是打分题，所以不给推荐指数。

### ✅ 增加：自定义单价

设置里可以填「输入 / 输出单价（元每百万 tokens）」，留空就用内置价格表。
DeepSeek 改价或换模型名后，不用改代码，也不用等我发新版本。

### ✅ 增加：数据来源标注与免责声明

- 排行榜顶部显示「**数据版本 v2025.1 · 更新于 2026-09-24**」
- 每条展开后的「依据」栏写明出自手册哪一节、或对应哪个国标编号
- 底部加了免责声明：不构成施工依据，以现行国标、当地规定和现场情况为准；
  涉及结构、燃气、电路的改动请交给有资质的单位

### 说明

- 网页现在 **486 KB**（0.1.8 是 410 KB）
- 仍是单文件、无外部依赖、无 CDN

---
## [0.1.8] — 2026-09-24 · 排行榜讲透了、能调色、能续写

### ✅ 排行榜：每条都「讲透」

- 每条新增三块内容，展开后不再只有六行：
  - **展开说清楚** —— 大白话详解（100~190 字 条），讲这是什么、为什么重要、
    常见做法、容易踩的坑
  - **依据** —— 引到《装修避坑手册（2025）》的具体部分，或对应的国标
    （GB 50242 给排水 / GB 50303 电气 / GB 50208 防水 / GB 50118 隔声 …）
  - **💡 经验提醒** —— 一句话点出最容易踩的那个坑
- **102 条全部重写补齐**，最短 100 字、最长 190 字；
  不写「适当」「合理」「注意」这类没有信息量的空话

### ✅ 回答长度：6 档，最高 2000 字

- 极简（150）/ 简短（300）/ 标准（600）/ 详细（1000）/ 很长（1500）/ **最长（2000）**
- 每档的输出额度同步放大（最长档 8000 tokens）

### ✅ 推荐产品：改成三态，默认「自动」

- **自动**（默认）：看这一条有没有开「🌐 联网」—— 开了就推荐
  （可以附上可核对的来源链接），没开就不推荐
- 也可以强制「总是开启」或「关闭」
- 旧版设置里存的「开 / 关」会自动迁移成「总是开启 / 自动」

### ✅ 页面配色：自动 / 白天 / 黑夜

- 默认「自动」跟随系统；也可以手动锁成「白天」或「黑夜」，
  这时会覆盖系统设置（滚动条、表单控件颜色一起跟着变）

### ✅ 被截断时可以「继续写完」

- 检测到回答撞到长度上限被强行截断时，直接在下面给一个
  **「继续写完」** 按钮，点一下接着往下写

### 说明

- 网页现在 **410 KB**（0.1.7 是 341 KB）—— 涨的都是排行榜的详解与依据
- 仍然是单文件、无外部依赖、无 CDN

---
## [0.1.7] — 2026-09-24 · 手册变数据、排行榜不再靠 AI

### ⛔ 取消（这一版**删掉**了这些）

- **删掉「装修避坑手册」页面**：首页与空间页的入口按钮、整页渲染、
  搜索框、相关事件与样式全部移除，导航与返回路径也一并清掉
- **手册原文不再嵌入网页**：原先内嵌的 4.9 万字手册正文整段删除，
  网页体积因此从 444 KB 回落到 **341 KB**
- **排行榜不再由 AI 生成**：删掉「生成排行榜 / 重新生成」按钮、联网生成、
  必须覆盖清单（RANK_COVERAGE）、生成提示词、返回校验与本地缓存、
  联网自检与诊断按钮 —— 打开即看，**不联网、不调用 AI、不花钱、不用等**
- 顺带清理：`state` 里 8 个只为生成服务的字段（rankData / rankLoading /
  rankError / rankSelfTest / rankSelfTestBusy / lastJsonDebug 等）

### ✅ 增加

- **内置排行榜数据 `RANK_DATA`**：8 个空间 × 3 个类别 = **102 条**，
  每条都是从《装修避坑手册》提炼成一句话判断，**不是搬运原文**
- **每条 9 个字段**：叫什么 / 为什么先做 / 正确做法 / 做错了会怎样 /
  怎么验收 / 参考花费 / 关键数字 / 问设计师的问题 / 五个维度评分
- **每条展开后两个按钮**：
  - **问设计师** —— 带着这一条进对话，自动把问题发出去
  - **联网搜索** —— 弹出独立窗口，去搜**别人家实际怎么做的**
    （论坛帖子、装修日记、知乎回答、真实案例），返回 3~5 条摘要 + 来源链接
- **搜索结果按条缓存**：下次点同一条直接看缓存，点「重新搜」再换一批；
  单条 8K 字符截断、单空间最多 30 条
- 条目依据标签统一为「**依据 · 装修避坑手册（2025）**」

### 说明

- 网页现在 **341 KB**（0.1.6 是 444 KB），仍是单文件、无外部依赖、无 CDN
- 排行榜页不再有加载态和失败提示 —— 它永远不会失败，因为不请求任何东西
- 切 tab / 切预设依旧只做本地重排

---
## [0.1.6] — 2026-09-24 · 内置《装修避坑手册（2025）》

### 新增 · 手册内置

- 把 **《装修避坑手册（2025）》正文**（94 页 / 4.9 万字）**内置进网页**：
  断网也能查，不依赖任何外部请求、不上传任何数据
- 首页与空间页都加入口「**📖 装修避坑手册（2025）**」，带搜索框 ——
  按关键词逐行命中（节流 250ms，输入时不丢焦点）；首页标题栏与面包屑也已接入

### 新增 · 排行榜真正引用手册

- 生成某张榜时，会按**该空间 + 该工程类别**从手册里检索相关原文
  （连前后文一起取，上限约 3200 字），连同「这是第一依据」的要求一起放进生成指令
- 依据类型新增「**手册**」：手册里有的说法就标「手册」并附对应小节名；
  手册没覆盖的才按经验补、标「经验」；有标准号的标「规范」
- 也就是说：同一个空间的榜，现在是从手册里长出来的，而不是全靠模型凭空发挥

### 说明

- 网页体积从 309 KB 增加到 **434 KB**（多出来的就是手册正文），
  仍是单文件、无外部依赖、无 CDN
- 手册只在你本地浏览器里，不联网也不上传

---
## [0.1.5] — 2026-09-24 · 更清楚、更具体

### 改进 · 首页卡片（手机上尤其明显）

- **整张卡片都是点击区**；卡片底部新增醒目的主色按钮「进入厨房 ›」，
  一眼就知道这才是正门（它只是提示元素，点击由整张卡片接手，不和快捷问题抢事件）
- 快捷问题移到卡片下半区，加虚线分隔 + 小标题「**大家常问：**」，
  按钮按文字宽度、不再占满整卡
- **按下就有反馈**：卡片轻微缩小、边框变主色、背景变浅 —— 手指按到哪是有感觉的

### 改进 · 空间页顺序

- 调整为「**最需要先解决的** → **聊一个具体的东西** → **大家最常问的**」
  进来先看到该先解决什么，而不是一上来就点问题

### 改名

- 「优先级排行榜」→「**最需要先解决的排行榜**」（应用栏标题、面包屑、页面标题、
  以及生成用的 prompt 全部同步）

### 改进 · 排行榜内容

- 新增「**正确做法 / 标准**」字段；每条展开后是六个说人话的部分：
  为什么先做 / 正确做法 / 做错了会怎样 / 怎么验收 / 参考花费 / 依据
- **去掉「返工 10 · 安全 10」这类只有数字的谜语**，改成人话解释
  （如「返工代价极高、涉及安全」），普通人不看分数也能懂
- 生成时要求**优先按《装修避坑手册（2025）》这类手册口径**写；
  手册没覆盖的才按经验补，并把依据标成「经验」，不许编造标准号
- 结果按空间**缓存在本机**：第一次生成后就是同一份，打开直接看，不会每次重算
- 条目里保留「**问设计师**」（带着这一条进对话，可继续联网查）与「复制这一条」
- 排行榜页底部加了一句：想更细就问设计师，在对话里开 🌐 联网继续查

### 新增 · 补充条件更全

- 从 3 项扩到 **13 项**：面积尺寸、层高、房屋类型（毛坯 / 旧房改造 / 精装房）、
  所在楼层、朝向、常住人口、有没有老人、有没有小孩、有没有宠物、
  预算档位、风格偏好、特殊需求（无障碍 / 隔音 / 智能家居 / 收纳）、一句话补充
- 都不填也能问；填了哪些就带进上下文，历史与云端一起存

---
## [0.1.4] — 2026-09-24

### 新增 · 优先级排行榜

把「钱该花在哪」从一句话变成一张可排序的榜单。

- 空间页新增入口卡「XX 的钱该花在哪」，进去是独立的排行榜页
- **三张榜分开生成**：硬装 · 隐藏 / 硬装 · 表面 / 软装
- 每条按**五个维度**打分（返工代价 / 安全影响 / 替换难度 / 使用频率 / 感知强度），
  加权算分后排成四个梯队：**S 做错要砸掉重来 · A 重点花钱 · B 够用就行 · C 可以后补**
- **四种预设权重**：均衡 / 家有老人小孩 / 预算紧 / 追求品质 —— 切换只本地重算，不发任何请求
- 结果**按空间缓存在本机**，切 tab、切预设、下次进入都不重新花钱
- 每条可展开看「为什么 / 关键数字 / 造价区间 / 做错的代价 / 怎么验」，
  并能「问设计师」（带着这条跳进对话）或「复制这一条」
- **不漏项的地基**：每个空间的「必须覆盖清单」（`RANK_COVERAGE`）原样塞进 prompt 要求逐条覆盖
- 返回的 JSON 一律先过 `sanitizeRank` 校验：分数夹到 0~10、校验造价区间与上下限、
  说「规范」却没给标准号的一律降级为「经验」、条目太少直接判无效
- 面包屑、应用栏标题、返回键、Esc 全部接入排行榜页

### 修复

- 空间页的 `class="chips row"` 写错了 —— `chips` 与 `row` 本该是同一个元素的
  两个 class，应为 `"chips.row"`

---

## [0.1.3] — 2026-09-24

### 新增 · 账号与云端历史

- **注册 / 登录**：接入 Supabase，邮箱 + 密码，走 GoTrue。登录态存在本机，
  下次打开用 `refresh_token` 免密续期并自动拉回云端历史
- **云端历史**：登录后对话历史存进 Supabase 的 `chat_history` 表，
  行级安全（RLS）按 `auth.uid()` 隔离 —— 每个账号只能读写自己的记录
- 未登录也能正常聊天，只是历史只留在本次页面里，关掉就没有
- 登录那一刻会把游客期间攒下的对话一起带上云端，不会白丢
- 退出登录清掉本机镜像（云端记录仍留在账号里）
- **可自建云端**：设置里新增「Supabase 服务器（高级，可选）」，
  可填自己的项目地址与可发布密钥，带「连接测试」和一份可折叠的建表 SQL
- 应用栏与设置里都有「退出登录」入口

### 修复

- 发送后待发图片缩略图一直挂在输入框上方（发送路径不重绘输入区）
- 从历史打开会丢掉 `sources` 与 `imageCount` —— 引用角标会变成死链
- 「整理成表 / 画个示意图」期间的「停止」按钮是假的
  （`jsonRequest` 的 AbortController 从没挂到 `state.controller`）
- 编辑态的用户气泡缺 `id`，定位很早的消息时会被甩到最底部
- 流式期间「更多」菜单被反复重建，打断正在悬停的菜单项
- 引用角标误吞正文里的序号（如「参考 [1] 号规范」）
- Key 校验遇 402 余额不足 / 429 限流时用了红字（Key 其实有效，改为中性色）
- `temperature` 与回答长度纳入快照；用量记账加会话守卫；历史用量过校验口
- `updateWebToggle` 的异常不再被静默吞掉

---

## [0.1.2] — 2026-09-24

### 修复

- **发消息后视口一路跑到最底端**：`scrollToMessageStart` 的 pin 会被自己
  触发的 `scroll` 事件撤销（`nearBottom()` 容差 120px 误判为贴底）。
  新增 `state.pinnedByCode`，只有用户真的动手（滚轮 / 触摸 / 拖动滚动条）才解除
- **新增设置「发出后视口」**：停在新回答的开头（默认）/ 一直跟到最新
- 点发送后画面先闪到底部再跳回回答开头（`settleKeyboard` 与 `rAF` 的顺序问题）
- 联网关键词提示永远不出现（`input` 里只改了状态、没刷新 DOM）
- 弹窗里 Key 输入框失焦后 `--kb` 可能残留（iOS）
- 位置保持式重绘会顺手把「贴底」状态解除

---

## [0.1.1] — 2026-09-24 · 手机体验

- 点发送后输入框悬在半空：`--kb` 只在 visualViewport 事件里更新，收键盘时不一定派发；加去重 + `settleKeyboard()` 多采样（0/60/140/260/420ms）
- 发完消息视口停在回答末尾：发送 / 重新生成后把视口钉在这条回答的开头（pin 即退出贴底），用户自己滚到底后恢复跟随
- 从历史进来总停在最上方：删掉 `keepPlace` 分支，现在统一落到最新一条消息的开头
- 「你还可以问」按钮占满整行、滑动误触：改 `align-items:flex-start` + `width:auto`，按钮宽度跟着文字走
- 点联网开关把阅读位置带走：改为原地更新按钮与提示，零重绘
- 新增 `renderKeepingScroll()`：开关面板、增删图片、整理 / 画图完成后精确还原滚动位置
- 回答长度改走快照，与推荐产品 / 思考强度一致
- `chatContent` 只有图时不塞空文本块

## [0.1.0] — 2026-09-24 · 体验版

- 修掉**发送键永久变灰**：`disabled` 只由模板写入、`updateControls` 从不清，任何一次 loading 期间的 render 都会把它永久留下；改为模板不写、每次显式复位
- 思维链：流式只显示「思考中 + 最近 3 行」，结束后收成一行横条「💭 思考了 8.4s · 展开」
- 联网检索状态条常驻回答上方：「🔍 正在检索…」→「🌐 已联网检索 5 条 · 2.3s」
- 「继续说完」：停止后按已写内容续写，续写指令只在当次请求里
- 快捷键：`Ctrl/Cmd + K` 设置、`Ctrl/Cmd + /` 速查、`Esc` 逐层退出、`↑/↓` 跳提问
- 图片缩略图点开看大图（纯前端灯箱）
- 导出与清空历史改用自家弹窗，不再用 `confirm`
- 空会话第一句话插进 `.chat-inner`（原来插到滚动容器外面看不见）
- 用量随历史存取，打开历史能看到那次会话的真实数字

## [0.0.9] — 2026-09-23 · 三栏固定布局

- 对话页改三栏：顶部常驻应用栏 + 中间独立滚动消息区 + 底部固定输入区
- 应用栏：返回 / 标题（空间 · 对象（话题））/ 副标题（状态 / 用量 / 档位 / 联网）/ 设置 / 更多菜单
- 消息区滚动条只在内部，顶栏与输入区始终可见
- 右下角浮动按钮：回到顶部 / 回到最新，离顶离底超一屏才出现
- 移除对话页大标题与面包屑；错误提示移到消息区顶部
- 用量从页面底部移到应用栏副标题
- 手机端弹窗改底部抽屉；历史搜索条吸顶
- 排版密度变量化：`--page-pad` / `--card-gap` / `--msg-gap` / `--bubble-pad`

## [0.0.8] — 2026-09-23 · 移动端与可验证

- 手机端重排：只压缩留白不缩小字号，卡片两列、气泡 96%
- 触屏 hover 复位；聚焦输入框不再被浏览器放大页面
- 字号可调（小 / 标准 / 大 / 特大），全站 rem + `--fs` 驱动
- 推荐产品联网版：允许品牌型号，但每条必须附真实来源链接，禁止编造 URL
- Markdown 链接渲染成可点网页
- 「整理成表 / 画示意图」不再静默失败，失败必提示、loading 必恢复
- 联网改走 Anthropic Messages 协议（`/anthropic/v1/messages` + `web_search_20250305`）
- 回答里 `[1] [2]` 渲染成可点角标，点了打开对应来源

## [0.0.7] — 2026-09-23 · 跟随 V4.1 Flash

- 模型名统一为 `deepseek-flash`，档位收敛为 4 个
- `payload.thinking` 由 `true` 改为 `{ type: "enabled" }`
- 新增 `reasoning_effort`：低 / 高 / 最高，默认 high
- 价格改为空闲 1 元 / 4 元（每百万 tokens），工作日高峰翻倍
- 新增「推荐产品」开关：只给类别、规格、价格区间，不给品牌型号
- 快照 `recommendProducts` / `reasoningEffort`，中途改设置不影响本条

## [0.0.6] — 2026-09-23 · 模型换代

- 模型档位制：快速 / 深度思考 / 旗舰 / 看图 / 自定义
- V4 混合思考：`thinking` 参数切思考，不再分两个模型名
- 联网搜索：输入区开关，走 `/responses` + `web_search`
- 思维链：解析 `reasoning_content`，可折叠「思考过程」块
- 图片输入：最多 4 张，压到 1600px 转 base64
- 「整理成表」：JSON 模式抽尺寸表 / 施工顺序 / 验收清单
- 「画个示意图」：JSON 模式取布局，前端 SVG 画平面图
- 用量统计：`stream_options.include_usage` + 本地按单价折算

## [0.0.5] — 2026-09-23 · 自查修复

- 重试流程不再凭空插一句「继续」污染对话
- reasoner 类模型不发 `temperature`，`max_tokens` 自动放大给思维链留额度
- HTTP 错误分类：401 / 402 / 429 / 5xx 各说各的
- 取消 / 停止后控件状态立即复位，不等下次渲染
- 清空历史同步清搜索框；未设 Key 的暂存问题覆盖前还原进输入框
- 历史搜索索引改 `id + updatedAt` 作 key，写库后仍命中
- 自动重试期间也落一次库，关页面不丢「问过什么」
- 流式结束后只重画最后一条气泡，长对话不整块重排
- 只认同源错误的全局兜底，插件报错不弹给用户

## [0.0.4] — 2026-09-23 · 设置与深色

- 深色模式（`prefers-color-scheme`）+ `prefers-reduced-motion` 降级
- 设置面板：模型、回答长度、temperature、空闲超时
- API Key 保存时校验有效性（区分无效 / 余额不足 / 无法验证）
- 单条回答重新生成；修改已发送的问题（改完自动重答）
- 导出 Markdown / 打印存 PDF；历史搜索 + 按空间筛选
- 回答末尾推荐 3 个追问，可直接点
- 条件新增「预算档位」；流式气泡尾部闪烁光标
- 键盘弹起用 `--kb` 顶起 composer / toast / 回底按钮
- 触屏 44px 触控区、`touch-action:manipulation`

## [0.0.3] — 2026-09-23 · 修 25 个问题

- 输入法回车不误发（`isComposing` / `keyCode 229`）
- 生成中按回车不吞字：先判断能发、再清空输入框
- 新增「停止生成」（AbortController）
- 中途离开的对话照样落库：用会话快照写历史，不看 `state.view`
- 流式离开页面不再索引越界：持有消息对象引用 + `abandoned` 标记
- 弹窗暂存问题用 `sessionId` + 时间戳校验，取消时还原回输入框
- 长对话按条数与字符预算裁剪，system 里告知省略条数
- 条件按空间分别保存，点 FAQ 不再清空已填尺寸
- 全站改事件委托 + `data-*`，无内联事件
- 表格判定放宽，流式中疑似表格行暂缓渲染
- `fetch` 空闲超时 45 秒，错误分类（超时 / 网络 / 服务端 / 空回复）

## [0.0.2] — 2026-09-23 · 改对话式

- 从「填表生成」改成「聊天追问」，`state.messages` 数组 + 全量历史发送
- 每个空间预置 6 条高频问题，点一下直接开问
- 条件表单砍到 2 项并折叠
- `SYSTEM_PROMPT` 改成四块结构：结论 / 为什么 / 对比表 / 买装坑
- 对话存 localStorage，首页可调回继续追问

## [0.0.1] — 2026-09-23 · 起点

- 三级导航：空间 → 设计对象 → 条件表单 → 生成建议
- 8 个空间、每个空间 6~10 个设计对象
- DeepSeek 流式输出，Markdown 轻量渲染
- API Key 存 localStorage，弹窗设置
